<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Whisky & Wheels</title>
        <link rel="stylesheet" href="/css/global.css" type="text/css">
        <link rel="stylesheet" href="/css/home.css" type="text/css">
    </head>
    <body>
        <header>
            <h1 class="u-container">
                <span>
                    Whisky
                </span>
                <span>
                    &amp;
                </span>
                <span>
                    Wheels
                </span>
            </h1>
        </header>
        <main class="u-container u-grid">
            {%- for trip in collections.trip -%}
                {# why is nunjucks formatting so weird? #}
                <a class="u-reset-link" href="{{ trip.page.url }}">
                    <article>
                        <h2 class="title">{{ trip.data.title }}</h2>
                        <h3 class="subtitle">{{ trip.data.subtitle }}</h3>
                        {% image ["img/", trip.data.image_file, ".jpeg"] | join, trip.data.image_alt, "300px" %}
                        <p class="days">
                            <strong>{{ trip.data.days }}</strong>
                            days</p>
                        <p class="kilometres">
                            <strong>{{ trip.data.kilometres }}</strong>
                            km</p>
                        <p class="location">From
                            <strong>{{ trip.data.start }}</strong>
                            to
                            <strong>{{ trip.data.end }}</strong>
                        </p>
                    </p>
                    <p class="excerpt">{{ trip.page.excerpt }}</p>
                </article>
            </a>
        {%- endfor -%}
    </body>
</html></main><footer>
<a href="/whisky">More about whisky</a>
<a href="/wheels">More about wheels</a></footer></body></html>