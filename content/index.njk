<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Whisky and wheels</title>
        <link rel="stylesheet" href="/css/global.css">
        {# set up proper style files #}
        {# proper image sizing and formatting, these are massive #}
        {# probably doesn't belong on background, should be picture element with alt and everything #}
            <style>
article::after {
    content: '';
    display: block;
    width: 100px;
    height: 100px;
    background-image: var(--image);
    background-position: center;
    background-size: cover;
}
h1 {
    display: grid;
    grid-template-columns: min-content 0.8em min-content;
    grid-template-rows: 1fr;
    align-items: center;
}
h1 span:nth-of-type(1) {
    grid-column: 1 / span 1;
    grid-row-start: 1;
    font-weight: 100;
}
h1 span:nth-of-type(3) {
    grid-row-start: 1;
    grid-column: 3 / span 1;
}
h1 span:nth-of-type(2) {
    color: oklch(83.67% 0.2954 153.53);
    font-weight: 400;
    opacity: 0.3;
    font-size: 1.5em;
    grid-column: 2 / 3;
    grid-row-start: 1;
    justify-self: center;
}
    </style>
    </head>
        <body>
            <header>
                <h1>
                    <span>
                Whisky
                </span>
                    <span>
                &amp;
                </span>
                    <span>
                Wheels
            </span>
        </h1>
        </header>
            <main>
                {%- for trip in collections.trip -%}
                {# why is nunjucks formatting so weird? #}
                    <a href="{{ trip.page.url }}"> <article style="--image: url('img/{{ trip.data.image }}.jpeg')">
                        <h2>{{ trip.data.title }}</h2>
                        <h3>{{ trip.data.subtitle }}</h3>
                        <p>{{ trip.data.days }} Days</p>
                        <p>{{ trip.data.kilometres }} Kilometres</p>
                        <p>From {{ trip.data.start }} to {{ trip.data.end }}</p>
                        <p>{{ trip.page.excerpt }}</p>
                    </article>
                </a>
            {%- endfor -%}
        </main>
        <footer>
            <a href="/whisky">More about whisky</a>
            <a href="/wheels">More about wheels</a>
        </footer>
    </body>
</html>