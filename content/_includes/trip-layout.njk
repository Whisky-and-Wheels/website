{# todo: logic for relative stylesheet for trip or trip/collection #}
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="../../css/global.css" type="text/css">
        <title>
            {% if tags[0] !== 'trip' %}
                {{ tags[0] | capitalize }} |
            {% endif %}
            {{ title }}</title>
    </head>
    <body>
        <header>
            <h1>{{ title }}</h1>
        </header>
        <main>
            {{ content | safe }}
        </main>
    </body>
</html>