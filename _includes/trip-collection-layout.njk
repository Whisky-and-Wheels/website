<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
    </head>
    <body>
        <h1>{{ title }}</h1>
        {%- for trip in collections[name] -%}
            <article>
                <h2>
                    <a href="{{ trip.page.url }}">{{ trip.data.title }}</a>
                </h2>
            </article>
        {%- endfor -%}
    </body>
</html>